public class Area {
    public int AreaOfShape(int side){
        return (6*(side*side));
    }
    public double AreaOfShape(double radius,double height, double pi)
    {
        return ((2*pi*radius*height)+(2*pi*radius*radius));
    }
}
